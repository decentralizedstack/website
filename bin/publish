#!/usr/bin/env coffee

ghpages = require 'gh-pages'
path = require 'path'

handle_error = (error) -> throw new Error error if error

dist = path.join __dirname, '..', 'dist'

options =
  dotfiles: true

ghpages.publish dist, options, handle_error
